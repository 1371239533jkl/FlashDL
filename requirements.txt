# 核心依赖
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0
PyQt6-sip>=13.6.0
requests>=2.31.0

# BT/磁力下载（如不需要可移除）
libtorrent>=2.0.0
libtorrent-windows-dll>=0.0.3; sys_platform == "win32"

# 网页视频嗅探（可选，已封禁，预留）
# yt-dlp>=2024.0.0
